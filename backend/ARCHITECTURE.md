# 🏗️ MediBro System Architecture

## Complete System Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                         MediBro System                          │
└─────────────────────────────────────────────────────────────────┘

┌──────────────────┐         ┌──────────────────┐         ┌──────────────────┐
│                  │         │                  │         │                  │
│  Mobile App      │◄────────┤   Backend API    │◄────────┤  Hardware        │
│  (React Native)  │         │   (Node.js)      │         │  Device (IoT)    │
│                  │         │                  │         │                  │
└──────────────────┘         └────────┬─────────┘         └──────────────────┘
                                      │
                                      │
                                      ▼
                            ┌──────────────────┐
                            │    MongoDB       │
                            │    Database      │
                            └──────────────────┘
```

## Data Flow Diagram

```
┌─────────────────────────────────────────────────────────────────────────┐
│                           User Journey                                  │
└─────────────────────────────────────────────────────────────────────────┘

1. REGISTRATION & LOGIN
   User (Mobile) → POST /api/auth/register → Backend → MongoDB
   User (Mobile) → POST /api/auth/login → Backend → Returns JWT Token

2. SETUP PROFILE
   User → PUT /api/auth/profile → Backend → Updates User in MongoDB
   User → POST /api/auth/complete-setup → Links Bot ID

3. ADD MEDICINE
   User → POST /api/medicines → Backend → Creates Medicine + MedicineLogs
   
4. VIEW SCHEDULE
   User → GET /api/logs/today → Backend → Query MedicineLogs → Display

5. HARDWARE SYNC
   Hardware → GET /api/hardware/schedule?botId=MD-BOT-01
   Backend → Query MedicineLogs → Returns Schedule
   Hardware → Dispenses Medicine at Scheduled Time
   Hardware → POST /api/hardware/update-status
   Backend → Updates MedicineLog Status → Decrements Stock

6. VIEW ANALYTICS
   User → GET /api/analytics/adherence → Backend → Calculates Stats
   User → GET /api/analytics/insights → Backend → AI Recommendations
```

## Backend Architecture

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         Backend Structure                               │
└─────────────────────────────────────────────────────────────────────────┘

                              ┌──────────────┐
                              │  server.js   │
                              │  (Express)   │
                              └──────┬───────┘
                                     │
                    ┌────────────────┼────────────────┐
                    │                │                │
           ┌────────▼─────┐  ┌──────▼──────┐  ┌─────▼──────┐
           │  Middleware  │  │   Routes    │  │   Config   │
           │              │  │             │  │            │
           │  • auth.js   │  │  • auth     │  │  • db.js   │
           │  • error.js  │  │  • medicine │  └────────────┘
           │  • validate  │  │  • logs     │
           └──────────────┘  │  • analytics│
                             │  • hardware │
                             └──────┬──────┘
                                    │
                          ┌─────────▼─────────┐
                          │   Controllers     │
                          │                   │
                          │  • authController │
                          │  • medicineCtrl   │
                          │  • logController  │
                          │  • analyticsCtrl  │
                          │  • hardwareCtrl   │
                          └─────────┬─────────┘
                                    │
                          ┌─────────▼─────────┐
                          │     Models        │
                          │   (Mongoose)      │
                          │                   │
                          │  • User           │
                          │  • Medicine       │
                          │  • MedicineLog    │
                          │  • Adherence      │
                          └─────────┬─────────┘
                                    │
                          ┌─────────▼─────────┐
                          │     MongoDB       │
                          │    Collections    │
                          └───────────────────┘
```

## Request/Response Flow

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    Example: Add Medicine Flow                           │
└─────────────────────────────────────────────────────────────────────────┘

1. Client Request
   ┌────────────────────────────────────────────────────────┐
   │ POST /api/medicines                                    │
   │ Headers: Authorization: Bearer <JWT_TOKEN>             │
   │ Body: {                                               │
   │   name: "Paracetamol",                               │
   │   dosage: "500mg",                                   │
   │   times: ["09:00", "18:00"],                        │
   │   slot: "A1",                                       │
   │   quantity: 60                                      │
   │ }                                                   │
   └────────────────────────────────────────────────────────┘
                          │
                          ▼
2. Middleware Layer
   ┌────────────────────────────────────────────────────────┐
   │ ✓ protect() - Verify JWT token                        │
   │ ✓ addMedicineValidation - Validate input              │
   │ ✓ validate() - Check validation results               │
   └────────────────────────────────────────────────────────┘
                          │
                          ▼
3. Route Handler
   ┌────────────────────────────────────────────────────────┐
   │ medicineRoutes.js                                      │
   │ → calls medicineController.addMedicine()               │
   └────────────────────────────────────────────────────────┘
                          │
                          ▼
4. Controller Logic
   ┌────────────────────────────────────────────────────────┐
   │ medicineController.addMedicine()                       │
   │ • Check if slot is available                           │
   │ • Create medicine in database                          │
   │ • Generate medicine logs for schedule                  │
   │ • Return success response                              │
   └────────────────────────────────────────────────────────┘
                          │
                          ▼
5. Database Operations
   ┌────────────────────────────────────────────────────────┐
   │ Medicine.create() → Insert medicine                    │
   │ MedicineLog.insertMany() → Create 14 logs (7 days)    │
   └────────────────────────────────────────────────────────┘
                          │
                          ▼
6. Response
   ┌────────────────────────────────────────────────────────┐
   │ 201 Created                                            │
   │ {                                                      │
   │   success: true,                                       │
   │   message: "Medicine added successfully",              │
   │   data: { ...medicine object... }                      │
   │ }                                                      │
   └────────────────────────────────────────────────────────┘
```

## Database Schema Relationships

```
┌─────────────────────────────────────────────────────────────────────────┐
│                      Database Relationships                             │
└─────────────────────────────────────────────────────────────────────────┘

┌──────────────┐
│    users     │
│──────────────│
│ _id          │◄──┐
│ username     │   │
│ password     │   │ References (userId)
│ name         │   │
│ connectedBot │   │
└──────────────┘   │
                   │
       ┌───────────┼───────────┬──────────────┐
       │           │           │              │
       ▼           ▼           ▼              ▼
┌──────────┐ ┌─────────────┐ ┌──────────┐ ┌──────────┐
│medicines │ │medicinelogs │ │adherences│ │(hardware)│
│──────────│ │─────────────│ │──────────│ │──────────│
│_id       │◄┤medicineId   │ │userId    │ │botId     │
│userId    │ │userId       │ │period    │ │(lookup)  │
│name      │ │medicineName │ │stats     │ └──────────┘
│dosage    │ │scheduledTime│ └──────────┘
│times[]   │ │status       │
│slot      │ │takenTime    │
│quantity  │ └─────────────┘
└──────────┘
```

## Authentication Flow

```
┌─────────────────────────────────────────────────────────────────────────┐
│                      JWT Authentication                                 │
└─────────────────────────────────────────────────────────────────────────┘

1. LOGIN
   User → POST /api/auth/login {username, password}
        → Backend verifies credentials
        → Generate JWT Token
        → Return {token, user data}

2. STORE TOKEN
   Mobile App → AsyncStorage.setItem('token', token)

3. AUTHENTICATED REQUESTS
   Mobile App → GET /api/medicines
              → Header: "Authorization: Bearer <token>"
              → Middleware: protect()
              → Verify token
              → Extract userId
              → Attach user to req.user
              → Continue to controller

4. TOKEN EXPIRY
   After 7 days → Token expires
   Mobile App → Receives 401 Unauthorized
              → Redirect to login screen
```

## Hardware Integration Flow

```
┌─────────────────────────────────────────────────────────────────────────┐
│                  Hardware ↔ Backend Communication                       │
└─────────────────────────────────────────────────────────────────────────┘

STEP 1: DEVICE REGISTRATION
Hardware → POST /api/hardware/register
         {botId: "MD-BOT-01", userId: "user_id"}
Backend → Updates user.connectedBotId
        → Returns confirmation

STEP 2: FETCH SCHEDULE (Every hour or on-demand)
Hardware → GET /api/hardware/schedule?botId=MD-BOT-01
Backend → Find user by botId
        → Query pending logs for next 24 hours
        → Mark logs as syncedToHardware
        → Return JSON:
          [{
            logId: "abc123",
            medicineName: "Paracetamol",
            dosage: "500mg",
            scheduledTime: "2024-01-01T09:00:00Z",
            slot: "A1"
          }]

STEP 3: DISPENSE MEDICINE (At scheduled time)
Hardware → Check current time
         → If time matches scheduledTime
         → Activate motor for slot A1
         → Dispense medicine
         → LED indicates success

STEP 4: UPDATE STATUS
Hardware → POST /api/hardware/update-status
         {botId: "MD-BOT-01", logId: "abc123", status: "dispensed"}
Backend → Find log
        → Update status to "taken"
        → Set takenTime
        → Decrement medicine.remaining
        → Return confirmation

STEP 5: MOBILE APP SYNC
User opens app → GET /api/logs/today
Backend → Returns updated logs
Mobile → Shows medicine marked as "Taken" ✓
```

## Complete User Flow Example

```
┌─────────────────────────────────────────────────────────────────────────┐
│              Day in the Life of MediBro User                            │
└─────────────────────────────────────────────────────────────────────────┘

08:00 AM - Wake Up
└─► Open MediBro App
    └─► GET /api/logs/today
        └─► See: 3 medicines today
            ├─► Paracetamol - 09:00 AM (Pending)
            ├─► Vitamin D - 02:00 PM (Pending)
            └─► Omega-3 - 08:00 PM (Pending)

08:45 AM - Hardware Checks Schedule
└─► Hardware polls: GET /api/hardware/schedule?botId=MD-BOT-01
    └─► Receives: Paracetamol at 09:00 AM, Slot A1

09:00 AM - Medicine Time!
└─► Hardware beeps
    └─► Dispenses from Slot A1
        └─► POST /api/hardware/update-status
            └─► Backend updates: Paracetamol marked as "Taken"
                └─► Medicine.remaining: 60 → 59

09:15 AM - User Checks App
└─► Open app
    └─► GET /api/logs/today
        └─► Paracetamol shows ✓ Taken at 09:05 AM

02:00 PM - Afternoon Dose
└─► Hardware dispenses Vitamin D (Slot A2)
    └─► Updates backend
        └─► Status: Taken ✓

08:00 PM - Evening Dose
└─► User busy, misses notification
    └─► 08:30 PM - Hardware still pending
        └─► User manually takes from dispenser
            └─► App: PUT /api/logs/{logId}/status {status: "taken"}

10:00 PM - Daily Summary
└─► User checks analytics
    └─► GET /api/analytics/adherence?period=week
        └─► Adherence: 95% this week
        └─► GET /api/analytics/insights
            └─► "Great job! You're consistent"
```

## Error Handling Flow

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        Error Handling                                   │
└─────────────────────────────────────────────────────────────────────────┘

Request → Middleware → Controller → Database
   │          │            │           │
   ▼          ▼            ▼           ▼
Error     Error        Error       Error
   │          │            │           │
   └──────────┴────────────┴───────────┘
                  │
                  ▼
        ┌─────────────────┐
        │  errorHandler   │
        │  middleware     │
        └────────┬────────┘
                 │
                 ▼
        ┌─────────────────┐
        │  Formatted      │
        │  Error Response │
        │  {              │
        │    success: false│
        │    message: "..." │
        │    errors: []   │
        │  }              │
        └─────────────────┘
```

---

## Summary

✅ **3-Tier Architecture**: Mobile → Backend → Database
✅ **RESTful API**: Clean, organized endpoints
✅ **Secure**: JWT auth, password hashing, validation
✅ **Real-time**: Hardware sync every hour
✅ **Smart**: Auto-scheduling, analytics, AI insights
✅ **Scalable**: Modular design, easy to extend

**Your complete system is ready to deploy!** 🚀
